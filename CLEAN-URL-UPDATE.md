# Clean URL + Worldwide Share Update — 2026-09-08

This build changes public internal URLs from `.html` paths to clean directory URLs:

- `/series/`
- `/book/`
- `/fault-lines/`
- `/authors/`
- `/press/`
- `/shop/`
- `/release-updates/`
- `/links/`

The old `.html` files remain as compatibility redirect stubs so existing bookmarks/indexed links do not dead-end.

Also included:
- equal visual treatment for every worldwide retailer region card (South Korea no longer gets a one-off border)
- a fresh worldwide social-share image and metadata on `/shop/`
- updated canonical URLs, Open Graph URLs, JSON-LD, breadcrumbs, sitemap, nav links, and analytics tracking for clean URLs
- cache-busted CSS/JS references

Deploy the contents of this folder to the GitHub Pages repo root.
